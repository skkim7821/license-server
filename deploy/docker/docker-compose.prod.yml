services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_DB: license_server
      POSTGRES_USER: license
      POSTGRES_PASSWORD: license
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U license -d license_server"]
      interval: 5s
      timeout: 5s
      retries: 20

  license-server:
    image: ghcr.io/${GHCR_NAMESPACE:-skkim7821}/license-server:${BACKEND_IMAGE_TAG:-main}
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      NODE_ENV: production
      PORT: 3000
      DATABASE_URL: postgresql://license:license@postgres:5432/license_server?schema=public
      ADMIN_EMAIL: ${ADMIN_EMAIL}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD}
      ADMIN_JWT_SECRET: ${ADMIN_JWT_SECRET}
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:' + (process.env.PORT || 3000) + '/health').then(r => process.exit(r.ok ? 0 : 1)).catch(() => process.exit(1))"]
      interval: 5s
      timeout: 5s
      retries: 24
      start_period: 15s

  admin-web:
    image: ghcr.io/${GHCR_NAMESPACE:-skkim7821}/license-server-admin-web:${ADMIN_WEB_IMAGE_TAG:-main}
    restart: unless-stopped
    depends_on:
      license-server:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1/health >/dev/null 2>&1 || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12

  edge-proxy:
    image: nginx:1.27-alpine
    restart: unless-stopped
    depends_on:
      license-server:
        condition: service_healthy
      admin-web:
        condition: service_healthy
    ports:
      - "80:80"
      - "443:443"
    environment:
      SERVER_NAME: ${SERVER_NAME:-_}
      SSL_CERT_PATH: ${SSL_CERT_PATH:-/etc/letsencrypt/live/lc.skkim.dev/fullchain.pem}
      SSL_KEY_PATH: ${SSL_KEY_PATH:-/etc/letsencrypt/live/lc.skkim.dev/privkey.pem}
    volumes:
      - ./nginx.edge.conf.template:/etc/nginx/templates/default.conf.template:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
      - /var/lib/letsencrypt:/var/lib/letsencrypt:ro
    healthcheck:
      test: ["CMD-SHELL", "if [ \"${ENABLE_HTTPS:-true}\" = \"1\" ] || [ \"${ENABLE_HTTPS:-true}\" = \"true\" ] || [ \"${ENABLE_HTTPS:-true}\" = \"yes\" ]; then wget -qO- --no-check-certificate https://127.0.0.1/health >/dev/null 2>&1; else wget -qO- http://127.0.0.1/health >/dev/null 2>&1; fi || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12

volumes:
  postgres_data:
